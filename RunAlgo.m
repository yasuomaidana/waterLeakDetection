function err=RunAlgo(dato,i)
    minD=500;
    if(i>minD)
        %Aqui va a ir el algoritmo, que no evaluara hasta que pase el
        %tiempo necesario para que corra el programa
        %error=detecta(datos)
        %
        %
        %
        %
        %
    end
    if i==1000
        disp('Example that runs')
    end
end
